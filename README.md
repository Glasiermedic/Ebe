# Ebe

Ebe is a memory, context, and reflection system built around connected Memory Stones.

## Current status

Initial FastAPI service created.

## Project structure

- `apps/api` — FastAPI backend
- `apps/web` — React frontend
- `infrastructure` — Docker and deployment configuration
- `docs` — architecture and product documentation
